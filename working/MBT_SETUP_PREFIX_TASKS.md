# Enforce the Work-Item Token Prefix: Tasks

Checklist for the WORK_SETUP prefix-enforcement work item.

* PR wrangling
  * create branch `setup-prefix-enforce` off the default branch
  * commit changes on branch
  * create draft PR
  * review code changes
  * review PR description against final code one last time
  * request feedback from reviewers
  * respond to review feedback
* testing
  * automated checks — see `MBT_SETUP_PREFIX_CLAUDE.md`
  * manual checks — capture steps in `MBT_SETUP_PREFIX_TESTING.md`
* docs
  * <none expected — instruction-only change>
* mini-brain closeout
  * no reversal; `WORK_SETUP` already intends the prefix — see `MBT_WORK_CLOSEOUT.md`
