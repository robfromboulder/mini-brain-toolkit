# Mini-Brain Toolkit: Dream Cycle Log

Append-only log of dream cycle runs. Each entry records which phases ran, what broke, what was unexpectedly easy or hard, the disconfirmation searches run in Phase 3, and what the executor would change about the instructions. Each run appends after a `---` separator.

---

# First recorded dream cycle — clean structure, one finding, then a full user-requested re-baseline and a three-round spot-check (2026-07-28)

**Session ID**: `2cf8e91e-07cd-46ef-9e8f-ba469216ab3e`
**Model / effort**: Opus 4.8 (1M context), `claude-opus-4-8[1m]` — effort **xhigh**.

Rob's session, Claude co-author. First run recorded against this log (it was header-only, so no prior-cycle orientation was possible — noted and proceeded). Rob asked for a full cycle but no commit or push — he reviews the working-tree diff himself. Outcome: Phase 1 clean, Phase 2 consistent, Phase 4 extracted one finding, and Phase 3 initially **napped** under the freshness gate — then, on Rob's explicit request, run in full as a user-requested re-baseline (a sanctioned override of the gate; see the Phase 3 addendum). Files edited: `MBT_FINDINGS.md` (V8→V9), and from the re-baseline `MBT_COMPARABLES.md` (V3→V4), `MBT_BIOLOGY.md` (V4→V5), `MBT_RESEARCH.md` (V9→V10). Still nothing committed.

## Phases run

- **Phase 1 — structural integrity: ran, all six checks pass.** Read-index ↔ disk (13/13 both directions, no orphans), version headers well-formed with no smuggled change-notes, namespace grep empty, cross-references all resolve, no working/archive path leakage. The one apparent anomaly — a `templates/PRODUCT_HOOK.md` mention — is in `MBT_LOG.md` describing the historical rename to `PROJECT_HOOK.md`; legitimate history in an append-only log, not a dangling pointer.
- **Phase 2 — dogfooding & self-consistency: ran, consistent, nothing flagged.** Teaching-artifact check: `MBT_PATTERN.md`'s ten principles map one-to-one (count, numbering, meaning) to `MBT_CHECK_BRAIN.md`'s scorecard; the file set agrees across PATTERN's stage tables, `MBT_CREATE_BRAIN.md`'s create/stage-3 tables, and the actual 12 `templates/` files + 6 `templates/work/` files (no orphaned or missing template; the DREAM_CYCLE/DREAM_LOG mapping holds); lifecycle rituals match. Dogfooding self-check: principles 1–10 present/appropriate, SCOPE↔APPROACH orthogonal with no solution bias, read-index blocks not interleaved. Open-question scrub: none of SCOPE's five open questions is answered or narrowed by the artifacts this phase read (4.2, templates↔reference sync, remains open — the dogfooding phase keeps *this* brain's PATTERN↔templates aligned but still doesn't propagate to external exemplars).
- **Phase 3 — follow-on research: napped under the gate, then run in full on user request.** The gate fired correctly (all registry dates 2026-07-15, 13 days old; no prior flag), so Phase 3 was first skipped. Rob then asked for the full re-baseline, which overrides the gate and lifts the sub-agent cap — **see the Phase 3 addendum and the verification addendum below** for the disconfirmation searches, the four agents, the results, and a direct spot-check that corrected one of the agents' own claims.
- **Phase 4 — extraction & reflow: ran, one finding extracted, no reflow changes.** Scanned all five LOG sessions (2026-07-24 → 2026-07-27) forward. Exactly one clean FINDINGS-shaped item was stranded: the **CHECK-procedure fail-stop-over-handoff** decision (2026-07-24), added to `MBT_FINDINGS.md`. The rest of the recent LOG is either already embodied in shipped files (the project-repo hook → APPROACH; the dream-cycle numeric budgets and the three upstreamed checks → DREAM_CYCLE) or is process/framing lineage that correctly lives only in the LOG. Reflow: no finding is stale-now-obvious, no overlap to merge, the flat list has no section drift.

## Cost & structure

- **Agents spawned: four**, all in the re-baseline (Phases 1/2/4 used zero — every file is under the ~500-line delegation threshold, so all reads were in the active context). The four parallel web-research agents (sonnet) covered the two registries; all four returned useful structured deltas, and — unusually — three surfaced proposed *corrections* to the registry, not just staleness (one of the three, Mem0, was itself wrong and was reverted on spot-check; see the verification addendum). The routine ~10-agent budget governed Phases 1/2/4; the re-baseline lifted it, and four was enough.
- **Relative to a product-brain cycle:** *before* the re-baseline, with Phase 3 napped, the cycle had collapsed to roughly a product brain's shape — structural + dogfooding + extraction, no web — plus the dual-nature Phase 2 teaching-artifact consistency check (PATTERN ↔ CREATE ↔ CHECK ↔ templates). The user-requested re-baseline then restored the full research-brain cost (four agents + three verification rounds), which is where most of this run's effort ultimately went — and where all its net-new value came from. That contrast is the signal: a routine research-brain cycle and a product-brain cycle look nearly identical once the nap gate fires; the difference only appears when the research phase actually runs.

## What I'd change about the instructions

- **The effort level isn't surfaced to the executor.** The instruction's fallback ("ask the user") worked, but it forced an interruption for a metadata field. Consider having the harness surface it, or accept a "not surfaced" value rather than blocking.
- **First-recorded-cycle vs. the nap gate.** On the *first* recorded cycle the dream log is empty, yet the registry dates can already be fresh (here, refreshed 13 days earlier during an ordinary session). The gate then skips Phase 3 entirely — so a *research* brain runs its very first logged cycle without ever exercising the adversarial thesis-attack that is its whole point. Worth deciding whether an empty dream log should force one Phase 3 baseline regardless of the gate. (Flagged below — though the nap-vs-run case is more nuanced than it first looked; see the verification addendum.)
- **Scoring a napped Phase 3.** Phase 5 says each Phase 3 confidence "must cite the disconfirmation search that earned it," but a napped phase runs no such search. The instructions should say how to score carried-forward Phase 3 conclusions (before the re-baseline I had capped them at 60, carried-not-earned; the re-baseline then let me earn them, and the spot-check moved them again — see the self-evaluation).

## Phase 3 addendum — full re-baseline (user-requested)

After the nap, Rob asked to run 3a/3b in full and fold the results in rather than redo the cycle. That request overrides the nap gate and lifts the sub-agent cap. Four parallel web-research agents (sonnet — one per landscape family plus one for cognitive science) were briefed adversarially, to *falsify* the load-bearing claims, so the new-entrant/new-research hunts doubled as the disconfirmation searches.

**Disconfirmation searches run (the auditable record):**
- *forgets/shrinks-by-design* — hunted for an adoption-leading system shipping editorial/human-curated forgetting, consolidation-with-deletion, or shrink-toward-core, across production memory (Mem0/Cognee/Zep/Letta/LangMem/MemOS/Caura/Vektor + benchmark leaderboards), coding-agent conventions, and human patterns. **Not falsified** — none occupies the human-authored-first editorial-shrink-over-project-docs intersection. Nearest approaches: Anthropic Managed Agents "Dreaming" (opt-in human gate over machine consolidation-with-deletion), ByteRover (human gate on writes), Basic Memory (human-editable markdown, personal scale).
- *human-curated not machine-managed* — same hunt, plus any published human-vs-machine measurement. **Boundary narrowing** (Anthropic's opt-in gate exists) but no head-to-head human-vs-machine study exists at all — absence reconfirmed.
- *shrink-toward-core is safe* — searched for evidence that shrinking loses information. **Partial caution found** — Ground Truth First's tenure-crossover shows *unqualified* eviction loses long-horizon recall, but it doesn't test shed-only-re-derivable.
- *biology is load-bearing* — searched 2026 cognitive science for findings breaking the two load-bearing verdicts. **Both weakened** — Tibon et al. (episodic/semantic neural null) and SWS/REM + Fountas (staged, not single-pass).

**Files updated by the re-baseline:** `MBT_COMPARABLES.md` V3→V4 (living entries re-verified 2026-07-28; Mem0/Vektor/Cline corrected; Copilot/GEMINI/Windsurf/Kilo/Roo/Auto-Dream refreshed; new rows: Anthropic Dreaming, Google Memory Bank, ByteRover, Basic Memory, Hindsight, Ground Truth First, the 2026 sleep-consolidation cluster, MemDelta/Anatomy, ICLR MemAgents, Google OKF). `MBT_BIOLOGY.md` V4→V5 (2026 research folded into seven fields; two candidate mechanisms added — two-phase dream cycle, predictive forgetting; caveats on both load-bearing mapping rows). `MBT_RESEARCH.md` V9→V10 (differentiator "holds but narrowed"; corrections; empirical evidence; thesis-level flags).

## Phase 3 verification addendum — direct spot-check

Because the re-baseline's facts came entirely through four Sonnet web-research agents (my own knowledge cutoff predates the dates involved), Rob asked me to spot-check the four highest-stakes claims directly (WebSearch/WebFetch in the active context) and fold in anything that moved a conclusion.

- **Anthropic Managed Agents "Dreaming"** (opt-in human gate over consolidation-with-deletion) — **confirmed** (claude.com/blog + multiple outlets). The positioning-narrowing flag stands.
- **Tibon et al. 2026** episodic/semantic neural null — **confirmed** (*Nature Human Behaviour* 10, 803–821; n=40, no neural difference). The biology-verdict flag stands.
- **Ground Truth First** (tenure crossover) — **confirmed** (arXiv 2607.21962; paper and eviction mechanism real; the exact percentages are the paper's reported figures, taken from the agent).
- **Mem0** — **the agent was wrong; my "correction" is reverted.** Mem0's own docs confirm **Platform v3 is single-pass ADD-only** ("nothing is overwritten or deleted," history preserved, memories accumulate); the sub-agent had conflated the legacy ADD/UPDATE/DELETE pipeline with the current default. The *original* registry claim was substantially right, so the confirming data point is restored, not lost.
- **Vektor** (re-checked as a correlated risk, since the same agent produced it) — **confirmed non-destructive** (vendor docs: archives to a "dream tier" via `rem_lineage`, "grows wiser, not larger"); a real but small/emerging product. Downgrade stands; "unadopted" softened to "small/emerging."

**Residual unverified (low-stakes, flagged agent-sourced):** minor state (Cognee auto-distill, Zep `remove_episode`, Kilo→AGENTS.md, etc.) and the biology candidate-mechanism arXiv IDs — the latter feed *flagged candidates* the user reviews before acting, so they get checked at that gate.

**Lesson (the most transferable output of this cycle):** sub-agent web results on a fast-moving field must be spot-checked before folding in — one confidently-wrong agent correction nearly *replaced a correct registry fact with a wrong one*. A verification step on agent-proposed **corrections** (not just new entrants) belongs in Phase 3.

**Second verification round** (Rob asked for broader coverage after the Mem0 miss). Five more high-leverage claims checked directly — **Letta** (MemFS is agent-concurrency + auto-curation, not human PR review), **MemOS** ("model-defined, not human-defined" stance), **ByteRover** (human gate on writes), **Basic Memory** (genuinely human-editable markdown), and **Fountas 2026** (compression "unattainable in a single pass," needs iterative refinement) — **all five confirmed.** Only one item softened: the **Copilot Memory "reason"/rationale field** could not be independently confirmed (its owner review/delete controls were), so flag #3's evidence is hedged. Net across both rounds: ~11 claims verified → **1 reversal (Mem0), 2 softenings (Vektor "unadopted," Copilot "reason" field), 0 further conclusion-reversals.** Agent 2's failure was concentrated in Mem0; its other claims held up. The differentiator and biology flags now rest on directly verified sources.

**Third round (the last two loose ends).** **Cline** — spot-check-verified **active** (docs present it as a current, recommended methodology; the stored "deprecated" was an *original-registry error*, now fixed). **GEMINI.md** — the "architectural rationale" framing is **confirmed but weak** (rationale-in-a-static-file, not a mechanism; possibly community rather than official framing). Final accounting across three rounds (~13 claims checked): the 13-day-stale registry held **2 real errors the re-baseline correctly caught** (Vektor framing, Cline "deprecated"); the re-baseline's agents **introduced 1** (Mem0, reverted by the spot-check); **1 stays hedged** (Copilot "reason" field). No load-bearing conclusion now rests on an unverified claim — the value of *running* the re-baseline (caught 2) and the value of *verifying* it (caught 1) both showed up.

## Needs human decision

1. **Confirm the one auto-applied extraction.** `MBT_FINDINGS.md` V8→V9 gained the CHECK fail-stop-vs-handoff finding (rejected handoff + state-change-under-auto-approval reason), from the 2026-07-24 session; it fits the FINDINGS format and is invisible from the shipped `MBT_CHECK_BRAIN.md`. Keep, or revert to LOG-only.
2. **Positioning must sharpen — the differentiator narrowed.** Anthropic's Managed Agents "Dreaming" now ships an *opt-in human review gate* over consolidation-with-deletion, so "nobody offers human-gated forgetting" is false. The differentiator still holds at the precise intersection — *human-authored-first editorial shrink-toward-irreducible-core over project-knowledge docs* — but `MBT_SCOPE`'s differentiator / `MBT_APPROACH`'s positioning / the README pillars are worded more broadly. Decision: re-word to the sharper claim? (Registries + research agenda are updated and flagged; the pattern/scope/approach/README are your call.)
3. **Rationale-capture claim is *weak* encroachment.** Spot-checks: the **GEMINI.md "architectural rationale" framing is confirmed** (but it's just rationale-in-a-static-file, not a mechanism, and possibly community rather than official framing); the **Copilot Memory "reason" field could not be confirmed** (its owner review/delete controls were). Neither matches curated, living, shrinking rationale. Decision: likely a low-priority watch, not a positioning change — but if you want, explicitly distinguish *narrated rationale in living docs* from *rationale-in-a-static-instruction-file*.
4. **Biology: should the semantic-docs verdict drop from load-bearing to mixed?** Tibon et al. 2026 (*Nature Human Behaviour*) found no neural episodic/semantic dissociation, contesting the *neural* basis of "canonical docs ↔ semantic memory" (the functional record→distillate mapping survives). I held the headline at 2-of-7 rather than auto-flip it. Decision: downgrade to 1-of-7, or keep 2 with the caveat? Also: `MBT_BIOLOGY.md` now carries two flagged candidate mechanisms (two-phase dream cycle; predictive-forgetting objective) and the surprise-gated-capture candidate is now strongly externally validated — each is a proposal for the roadmap or the pattern, your call.
5. **Should a shrink floor be named?** Ground Truth First quantifies that *unqualified* eviction loses long-horizon recall. It doesn't test the mini-brain's shed-only-re-derivable rule, but it strengthens the standing "when does shrinking go too far?" question toward an explicit floor. Decision: leave it to principle 1, or name a floor.
6. **Two candidate findings I did *not* extract.** Left as LOG lessons (craft/framing, not build-decisions): (a) *"Instructions that add to an agent's brief after its spawn point are un-executable in the order written"*; (b) *"A public/README claim is a toolkit obligation."* Promote into FINDINGS if you want them durable.
7. **Nap-gate tuning (instruction change).** The gate would have skipped this re-baseline on 13-day-stale dates — which held **two real errors** (Vektor mis-framing; Cline stored as "deprecated" but actually active). But the spot-check also showed the *re-baseline itself* introduced a fresh error (the Mem0 sub-agent claim), so "run more often" is not unambiguously safer without verification. Decision: add a "first recorded cycle forces one Phase 3 baseline" clause and/or shorten the staleness window, and/or require a spot-check on agent-proposed corrections — or accept the 30-day rotation plus user-invoked re-baseline.

## Self-evaluation

- **Confidence — Phase 2** (teaching artifacts self-consistent, toolkit still obeys its own pattern, no open question overtaken unnoticed): **90.** Cap: principles matched by count/numbering/meaning, not a word-for-word gloss diff; file set confirmed against a live `ls`.
- **Confidence — Phase 3a** (forgets/shrinks-by-design still holds): **85 — raised from a mid-cycle 80 by the spot-check.** The adversarial hunt across production memory, coding conventions, human patterns, and benchmark leaderboards found no adoption-leading editorial-shrink system; the three-round spot-check then *directly verified* every nearest-threat as not occupying the intersection (Anthropic Dreaming is opt-in and over agent memory; Mem0 v3 accumulates; Vektor is non-destructive; ByteRover gates writes not forgetting; Basic Memory is personal-scale). The mid-cycle 80 was capped by "this rests on unverified agent retrieval" — verifying the load-bearing facts removed exactly that cap. Held below ~90 by the confirmed narrowing (Anthropic's opt-in gate) and the field's speed.
- **Confidence — Phase 3b** (biological grounding is load-bearing, not flattering): **66 — nudged down from a mid-cycle 68, and now earned on *verified* disconfirmation.** The spot-check confirmed both weakenings are real, not agent-hallucinated (Tibon's episodic/semantic neural null in *Nature Human Behaviour*; Fountas's "unattainable in a single pass"). Unlike 3a, verification here resolved the prior two-sided uncertainty in the *pessimistic* direction — the weakenings held up — so the grounding reads as only partially load-bearing, leaning more flattering than the mid-cycle estimate. Below 70 with less remaining doubt than before; the "independently discoverable without biology" evidence (Anthropic Dreaming) still cuts against the grounding being load-bearing at all.
- **Confidence — finding extracted** (CHECK fail-stop): **74.** Cap: the "belongs in FINDINGS vs. LOG-only" judgment is Rob's; format-fit and invisibility-from-files earned the score.
- **New ground vs. re-tread:** **net-new, emphatically.** The re-baseline refined the Vektor framing (non-destructive, small), surfaced the first shipped human-gated-forgetting product (Anthropic Dreaming), added two biological candidate mechanisms, and weakened both load-bearing biology verdicts — and a direct spot-check then *reversed* one of its own agent-proposed corrections (Mem0). Not a re-tread.
- **Depth achieved:** all four phases ran to intent; Phase 3 ran deep once the gate was overridden (four agents, full delta pass over both registries plus new-entrant/new-research hunts). Two agent results carried caveats I preserved rather than overclaimed: MemClaw/Caura's adoption scale and the ICLR best-paper list could not be verified.
- **Diminishing returns:** N/A — first recorded cycle, and high-yield. Signal for next time: the nap gate would have skipped this on 13-day-stale state that did hold two real errors (Vektor mis-framing; Cline wrongly marked deprecated) — but the re-baseline also introduced one error of its own (Mem0) that only a direct spot-check caught, so gate-tuning must pair "run more often" with "verify agent corrections" (flag 7).
