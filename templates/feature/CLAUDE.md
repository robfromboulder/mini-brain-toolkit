# <Feature Name>: Claude Runbook

Instructions for feature branch: `<feature-branch>`

## Running Tests

Run the steps from the platform runbook first (if the brain has one), verifying results match the expectations documented there. Then run the feature-specific step(s) below.

### Step N — <feature-specific test> ← NEW in this branch

```bash
# fill in once the feature has tests
```

**Expected:** <runtime, pass/fail expectations, which tests are active>
