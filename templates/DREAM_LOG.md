# <Project>: Dream Cycle Log

Append-only log of dream cycle runs. Each entry records which phases ran, what broke or surprised, how many agents were spawned, and what the executor would change about the instructions. Each run appends after a `---` separator.
